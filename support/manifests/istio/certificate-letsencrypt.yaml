apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: always-open-tls
  namespace: istio-system
spec:
  secretName: always-open-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
  - "app.bealwaysopen.com"
