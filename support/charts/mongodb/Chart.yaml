apiVersion: v2
name: mongodb
version: 0.1.0
description: Wrapper chart to deploy MongoDB for dev

type: application

dependencies:
  - name: mongodb
    version: "~15.6.0"
    repository: "https://charts.bitnami.com/bitnami"
